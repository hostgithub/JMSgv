<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="180dp"
    android:layout_height="150dp"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical"
    android:padding="2dp">

    <ImageView
        android:id="@+id/image"
        android:layout_width="170dp"
        android:layout_height="140dp" android:scaleType="centerCrop"
        android:gravity="center"
        tools:src="@drawable/ic_image_loading"/>

    <TextView
        android:id="@+id/textview"
        android:layout_width="170dp"
        android:layout_height="wrap_content" android:background="#696969"
        android:layout_alignLeft="@+id/image"
        android:layout_alignBottom="@+id/image" android:layout_marginLeft="3dp"
        android:text="123" android:singleLine="true"
        android:textSize="18sp" android:textColor="@color/colorWhite"/>
</RelativeLayout>